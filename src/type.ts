export interface CategoryType {
  name: string;
}

export interface FreelanceRegisterFieldType {
  fname: string;
  lname: string;
  village: string;
  district: string;
  province: string;
  dob: Date;
  documentType: string;
}
